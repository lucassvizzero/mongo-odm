image: docker:stable

services:
  - docker:dind

tests:
  image: python:3.6-alpine
  only:
    refs:
      - master
      - develop
  stage: test
  coverage: '/\d+\%\s*$/'
  script:
    - pip install pytest coverage
    - coverage run --source jormungandr -m pytest
    - coverage report
